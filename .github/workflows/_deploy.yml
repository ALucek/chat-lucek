name: _deploy
on:
  workflow_call:
    inputs:
      sha:
        description: Commit SHA to check out, build, tag, and deploy
        type: string
        required: true
      deploy_api:
        type: boolean
        required: true
      deploy_web:
        type: boolean
        required: true
      deploy_agent:
        type: boolean
        required: true

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: dev
    permissions:
      contents: read
      id-token: write # WIF auth to GCP
      attestations: write # sign build provenance + SBOM
    env:
      IMAGE_TAG: ${{ inputs.sha }}
      REGISTRY: ${{ vars.GCP_REGION }}-docker.pkg.dev/${{ vars.GCP_PROJECT }}/chat
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          ref: ${{ inputs.sha }}
      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        id: auth
        with:
          workload_identity_provider: ${{ vars.WIF_PROVIDER }}
          service_account: ${{ vars.DEPLOY_SA }}
          # OIDC token to reach the IAP-gated dev host (audience = client id).
          token_format: id_token
          id_token_audience: ${{ vars.IAP_OAUTH_CLIENT_ID }}
          id_token_include_email: true
      - uses: google-github-actions/setup-gcloud@aa5489c8933f4cc7a4f7d45035b3b1440c9c10db # v3.0.1
      - run: gcloud auth configure-docker ${{ vars.GCP_REGION }}-docker.pkg.dev --quiet
      - name: Build and push api
        id: build_api
        if: ${{ inputs.deploy_api }}
        run: |
          docker build -t "$REGISTRY/api:$IMAGE_TAG" ./api
          docker push "$REGISTRY/api:$IMAGE_TAG"
          digest=$(docker inspect --format='{{index .RepoDigests 0}}' "$REGISTRY/api:$IMAGE_TAG" | cut -d@ -f2)
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
      - name: Build and push web
        id: build_web
        if: ${{ inputs.deploy_web }}
        run: |
          docker build \
            --build-arg NEXT_PUBLIC_GOOGLE_CLIENT_ID="${{ vars.GOOGLE_CLIENT_ID }}" \
            -t "$REGISTRY/web:$IMAGE_TAG" ./web
          docker push "$REGISTRY/web:$IMAGE_TAG"
          digest=$(docker inspect --format='{{index .RepoDigests 0}}' "$REGISTRY/web:$IMAGE_TAG" | cut -d@ -f2)
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
      - name: Build and push agent
        id: build_agent
        if: ${{ inputs.deploy_agent }}
        run: |
          docker build -t "$REGISTRY/agent:$IMAGE_TAG" ./agent
          docker push "$REGISTRY/agent:$IMAGE_TAG"
          digest=$(docker inspect --format='{{index .RepoDigests 0}}' "$REGISTRY/agent:$IMAGE_TAG" | cut -d@ -f2)
          echo "digest=$digest" >> "$GITHUB_OUTPUT"
      - name: Run migrations
        if: ${{ inputs.deploy_api }}
        run: |
          gcloud run jobs update chat-migrate \
            --image "$REGISTRY/api:$IMAGE_TAG" --region "${{ vars.GCP_REGION }}"
          gcloud run jobs execute chat-migrate --region "${{ vars.GCP_REGION }}" --wait
      # Deploy candidates with no traffic; a failed probe fails the step.
      - name: Deploy agent candidate
        if: ${{ inputs.deploy_agent }}
        run: |
          gcloud run services update chat-agent \
            --image "$REGISTRY/agent:$IMAGE_TAG" --region "${{ vars.GCP_REGION }}" \
            --no-traffic --tag cand
      - name: Deploy api candidate
        if: ${{ inputs.deploy_api }}
        run: |
          gcloud run services update chat-api \
            --image "$REGISTRY/api:$IMAGE_TAG" --region "${{ vars.GCP_REGION }}" \
            --no-traffic --tag cand
      - name: Deploy web candidate
        if: ${{ inputs.deploy_web }}
        run: |
          gcloud run services update chat-web \
            --image "$REGISTRY/web:$IMAGE_TAG" --region "${{ vars.GCP_REGION }}" \
            --no-traffic --tag cand
      # Verify api/web candidates on the dev host; the agent is smoked below.
      - name: Verify candidates on the dev host
        env:
          TOKEN: ${{ steps.auth.outputs.id_token }}
          DEV: https://dev.${{ vars.DOMAIN }}
        run: |
          set -euo pipefail
          if [ "${{ inputs.deploy_api }}" = "true" ]; then
            ready=$(curl -s -o /dev/null -w '%{http_code}' -H "Authorization: Bearer $TOKEN" "$DEV/readyz")
            test "$ready" = "200" || { echo "api candidate /readyz -> $ready"; exit 1; }
            me=$(curl -s -o /dev/null -w '%{http_code}' -H "Authorization: Bearer $TOKEN" "$DEV/api/me")
            test "$me" = "401" || { echo "api candidate /api/me -> $me"; exit 1; }
          fi
          if [ "${{ inputs.deploy_web }}" = "true" ]; then
            web=$(curl -s -o /dev/null -w '%{http_code}' -H "Authorization: Bearer $TOKEN" "$DEV/")
            test "$web" = "200" || { echo "web candidate / -> $web"; exit 1; }
          fi
      # Resolve the agent's base + candidate URLs for a live smoke.
      - name: Resolve agent candidate URL
        id: agent_url
        if: ${{ inputs.deploy_agent }}
        run: |
          set -euo pipefail
          j=$(gcloud run services describe chat-agent \
            --region "${{ vars.GCP_REGION }}" --format=json)
          echo "cand=$(echo "$j" | jq -r '.status.traffic[]|select(.tag=="cand").url')" >> "$GITHUB_OUTPUT"
          echo "base=$(echo "$j" | jq -r '.status.url')" >> "$GITHUB_OUTPUT"
      # Cloud Run wants the base URL as the audience, not the tag URL.
      - uses: google-github-actions/auth@7c6bc770dae815cd3e89ee6cdf493a5fab2cc093 # v3.0.0
        id: agent_token
        if: ${{ inputs.deploy_agent }}
        with:
          workload_identity_provider: ${{ vars.WIF_PROVIDER }}
          service_account: ${{ vars.DEPLOY_SA }}
          token_format: id_token
          id_token_audience: ${{ steps.agent_url.outputs.base }}
          id_token_include_email: true
      # Live inference on the candidate: assert a real answer, else no promote.
      - name: Smoke agent candidate
        if: ${{ inputs.deploy_agent }}
        env:
          CAND: ${{ steps.agent_url.outputs.cand }}
          TOKEN: ${{ steps.agent_token.outputs.id_token }}
        run: |
          set -euo pipefail
          body='{"messages":[{"role":"user","content":"What is your name?"}]}'
          out=$(curl -sS -X POST "$CAND/run" \
            -H "Authorization: Bearer $TOKEN" \
            -H 'Content-Type: application/json' -d "$body")
          echo "$out" | grep -q '^event: error' && { echo "agent error frame"; exit 1; }
          echo "$out" | grep -A1 '^event: delta' | grep -q '"text": *"[^"]' \
            || { echo "no non-empty delta"; exit 1; }
          echo "$out" | grep -A1 '^event: usage' \
            | grep -Eq '"output": *[1-9]' || { echo "usage output not > 0"; exit 1; }

      # Run last so an attestation hiccup never blocks the dev deploy.
      - name: Generate api SBOM
        if: ${{ inputs.deploy_api }}
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          image: ${{ env.REGISTRY }}/api:${{ env.IMAGE_TAG }}
          format: spdx-json
          output-file: api.sbom.spdx.json
          artifact-name: api-sbom.spdx.json
      - name: Attest api provenance
        if: ${{ inputs.deploy_api }}
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1
        with:
          subject-name: ${{ env.REGISTRY }}/api
          subject-digest: ${{ steps.build_api.outputs.digest }}
      - name: Attest api SBOM
        if: ${{ inputs.deploy_api }}
        uses: actions/attest-sbom@c604332985a26aa8cf1bdc465b92731239ec6b9e # v4.1.0
        with:
          subject-name: ${{ env.REGISTRY }}/api
          subject-digest: ${{ steps.build_api.outputs.digest }}
          sbom-path: api.sbom.spdx.json

      - name: Generate web SBOM
        if: ${{ inputs.deploy_web }}
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          image: ${{ env.REGISTRY }}/web:${{ env.IMAGE_TAG }}
          format: spdx-json
          output-file: web.sbom.spdx.json
          artifact-name: web-sbom.spdx.json
      - name: Attest web provenance
        if: ${{ inputs.deploy_web }}
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1
        with:
          subject-name: ${{ env.REGISTRY }}/web
          subject-digest: ${{ steps.build_web.outputs.digest }}
      - name: Attest web SBOM
        if: ${{ inputs.deploy_web }}
        uses: actions/attest-sbom@c604332985a26aa8cf1bdc465b92731239ec6b9e # v4.1.0
        with:
          subject-name: ${{ env.REGISTRY }}/web
          subject-digest: ${{ steps.build_web.outputs.digest }}
          sbom-path: web.sbom.spdx.json

      - name: Generate agent SBOM
        if: ${{ inputs.deploy_agent }}
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          image: ${{ env.REGISTRY }}/agent:${{ env.IMAGE_TAG }}
          format: spdx-json
          output-file: agent.sbom.spdx.json
          artifact-name: agent-sbom.spdx.json
      - name: Attest agent provenance
        if: ${{ inputs.deploy_agent }}
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1
        with:
          subject-name: ${{ env.REGISTRY }}/agent
          subject-digest: ${{ steps.build_agent.outputs.digest }}
      - name: Attest agent SBOM
        if: ${{ inputs.deploy_agent }}
        uses: actions/attest-sbom@c604332985a26aa8cf1bdc465b92731239ec6b9e # v4.1.0
        with:
          subject-name: ${{ env.REGISTRY }}/agent
          subject-digest: ${{ steps.build_agent.outputs.digest }}
          sbom-path: agent.sbom.spdx.json
